# Sprints

Create a directory after Sprint Planning establishes a Sprint Goal. Use `sprint-0001-three-word-goal/` and do not rename it after the Sprint begins.

Each Sprint directory contains `developer-plan.md` and `sprint-backlog/`. The Sprint Backlog contains `index.md`, `sprint-goal.md`, selected PBIs, Bugs, and other work artifacts.
